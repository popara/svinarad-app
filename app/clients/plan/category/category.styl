.category
.venue
  flow(column)
  .narrow-container
    width 100%

  nav
    flow(row)
    fill white
    justify-content space-between
    align-items center
    padding 10px 8px

    button
      flow(row)
      padding 0
      height 40px
      width 30px
      margin 10px

      svg
        width 100%
        height 100%
