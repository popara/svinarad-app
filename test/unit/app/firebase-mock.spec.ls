describe "FirebaseMocking" (,) !->
# before-each !->
#   module ($provide) !->
#     $provide.factory '$firebaseObject' -> {
#       $extend: -> it
#     }
#
# before-each inject ($firebaseObject) !->
#   fo := $firebaseObject
